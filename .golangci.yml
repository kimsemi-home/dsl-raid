version: "2"

run:
  timeout: 5m

linters:
  default: standard
  exclusions:
    paths:
      - apps/viewer/node_modules

issues:
  max-issues-per-linter: 0
  max-same-issues: 0

formatters:
  enable:
    - gofmt
    - goimports
  exclusions:
    paths:
      - apps/viewer/node_modules
