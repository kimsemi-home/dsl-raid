# Passes

Ordinary compiler passes over typed data.

Expected files:

- `normalize.lisp`
- `compose.lisp`
- `project.lisp`
- `diagnostics.lisp`

Passes should be individually testable and deterministic.

