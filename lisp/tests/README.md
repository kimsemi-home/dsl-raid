# Tests

Golden tests and pass-level tests for the Lisp authoring layer.

Expected file:

- `golden.lisp`
- `language.lisp` keeps shared assertions only.
- `language-*.lisp` files hold one diagnostic-family smoke test each.
