# Timeline

Timeline displays runtime trace events and coverage overlays.

It should consume trace and coverage data, then highlight observed states and
transitions through selection/hover or overlay state.

