# App Shell

Application shell, routes, command palette wiring, panel layout, and file
loading belong here.

The app shell consumes stores and view models. It should not perform FSM
analysis or mutate Core IR semantics.

Expected files:

- `shell.ts`
- `routes.ts`

